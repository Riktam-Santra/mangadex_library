enum SearchOrders {
  title,
  year,
  createdAt,
  updatedAt,
  latestUploadedChapter,
  followedCount,
  relevance,
  rating,
}

enum ChapterOrders {
  createdAt,
  updatedAt,
  publishAt,
  readableAt,
  volume,
  chapter,
}

enum CoverOrders {
  createdAt,
  updatedAt,
  volume,
}

enum OrderDirections {
  ascending,
  descending,
}

enum ScanlationOrders {
  name,
  createdAt,
  updatedAt,
  followedCount,
  relevance,
}
